// Web stub — on web the DOM editor in ../markImage.js (markImageWeb) is used
// directly, so this component renders nothing (and never imports native libs).
export default function MarkImageModal() { return null; }
